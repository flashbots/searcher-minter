import { sendFlashbotsBundle } from "../src/utils";

describe("blocknative mempool interaction", () => {
  it("connects to blocknative", () => {

    // TODO: Configure Blocknative and expect success

    expect(1).toBe(1);
  });
});